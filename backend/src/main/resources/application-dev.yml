# Activate with: mvn spring-boot:run -Dspring-boot.run.profiles=dev
# Seeds demo data on first run (see DataSeeder.java for credentials).
spring:
  jpa:
    show-sql: true
logging:
  level:
    com.company.studentmanagement: DEBUG
